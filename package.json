{
  "name": "registrar-backend",
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "start": "node src/index.js",
    "build": "npx playwright install --with-deps chromium"
  },
  "dependencies": {
    "express": "^5.0.0",
    "playwright": "^1.48.0"
  }
}
